<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE topic SYSTEM "https://resources.jetbrains.com/writerside/1.0/xhtml-entities.dtd">
<topic xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="https://resources.jetbrains.com/writerside/1.0/topic.v2.xsd" title="Library Available Locales" id="lists-available-locales" is-library="true"><snippet id="available-locales"><chapter title="Afrikaans" id="lists-available-locales-af"><p>Hoe om Afrikaanse lokalisering by 'n Laravel-toepassing te voeg? Om dit te doen, moet u die konsole-opdrag hieronder skakel:</p><p>How to add Afrikaans localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% af</code-block></chapter><chapter title="Shqip" id="lists-available-locales-sq"><p>Si të shtoni lokalizimin shqip në një aplikacion Laravel? Për ta bërë këtë, duhet të telefononi komandën e konsolës më poshtë:</p><p>How to add Albanian localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% sq</code-block></chapter><chapter title="አማርኛ" id="lists-available-locales-am"><p>ወደ ላራቬል አፕሊኬሽን የአማርኛ ቋንቋን እንዴት መጨመር ይቻላል? ይህንን ለማድረግ ከዚህ በታች ያለውን የኮንሶል ትዕዛዝ መደወል ያስፈልግዎታል:</p><p>How to add Amharic localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% am</code-block></chapter><chapter title="العربية" id="lists-available-locales-ar"><p>كيفية إضافة الترجمة العربية إلى تطبيق Laravel؟ للقيام بذلك، تحتاج إلى استدعاء أمر وحدة التحكم أدناه:</p><p>How to add Arabic localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% ar</code-block></chapter><chapter title="Հայերեն" id="lists-available-locales-hy"><p>Ինչպե՞ս ավելացնել հայերեն տեղայնացում Laravel հավելվածում: Դա անելու համար դուք պետք է զանգահարեք ստորև նշված վահանակի հրամանը.</p><p>How to add Armenian localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% hy</code-block></chapter><chapter title="অসমীয়া" id="lists-available-locales-as"><p>লাৰাভেল এপ্লিকেচনত অসমীয়া স্থানীয়কৰণ কেনেকৈ যোগ কৰিব? ইয়াৰ বাবে, আপুনি তলৰ ক'ন্সোল আদেশ কল কৰিব লাগিব:</p><p>How to add Assamese localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% as</code-block></chapter><chapter title="Azərbaycan" id="lists-available-locales-az"><p>Laravel tətbiqinə Azərbaycan lokalizasiyasını necə əlavə etmək olar? Bunu etmək üçün aşağıdakı konsol əmrinə zəng etməlisiniz:</p><p>How to add Azerbaijani localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% az</code-block></chapter><chapter title="Bamanakan" id="lists-available-locales-bm"><p>Bamanankan sigidako fara Laravel baarakɛminɛn kan cogo di? Walasa k’o kɛ, aw ka kan ka konsolɛ cikan wele min bɛ duguma:</p><p>How to add Bambara localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% bm</code-block></chapter><chapter title="Euskara" id="lists-available-locales-eu"><p>Nola gehitu euskarazko lokalizazioa Laravel aplikazio bati? Horretarako, beheko kontsolaren komandora deitu behar duzu:</p><p>How to add Basque localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% eu</code-block></chapter><chapter title="Беларуская" id="lists-available-locales-be"><p>Як дадаць беларускую лакалізацыю ў дадатак Laravel? Каб зрабіць гэта, вам трэба выклікаць кансольную каманду ніжэй:</p><p>How to add Belarusian localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% be</code-block></chapter><chapter title="বাংলা" id="lists-available-locales-bn"><p>কিভাবে একটি Laravel অ্যাপ্লিকেশনে বাংলা স্থানীয়করণ যোগ করবেন? এটি করার জন্য, আপনাকে নীচের কনসোল কমান্ডটি কল করতে হবে:</p><p>How to add Bangla localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% bn</code-block></chapter><chapter title="भोजपुरी" id="lists-available-locales-bho"><p>लारावेल एप्लीकेशन में भोजपुरी लोकलाइजेशन कईसे जोड़ल जाला? एकरा खातिर रउआँ के नीचे दिहल कंसोल कमांड के कॉल करे के पड़ी:</p><p>How to add Bhojpuri localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% bho</code-block></chapter><chapter title="Bosanski" id="lists-available-locales-bs"><p>Kako dodati lokalizaciju na bosanski jezik u Laravel aplikaciju? Da biste to učinili, morate pozvati naredbu konzole ispod:</p><p>How to add Bosnian localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% bs</code-block></chapter><chapter title="Български" id="lists-available-locales-bg"><p>Как да добавим българска локализация към приложение на Laravel? За да направите това, трябва да извикате конзолната команда по-долу:</p><p>How to add Bulgarian localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% bg</code-block></chapter><chapter title="Català" id="lists-available-locales-ca"><p>Com afegir localització en català a una aplicació Laravel? Per fer-ho, heu de trucar a l'ordre de consola següent:</p><p>How to add Catalan localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% ca</code-block></chapter><chapter title="Cebuano" id="lists-available-locales-ceb"><p>Giunsa pagdugang ang lokalisasyon sa Cebuano sa usa ka aplikasyon sa Laravel? Aron mahimo kini, kinahanglan nimo nga tawagan ang console command sa ubos:</p><p>How to add Cebuano localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% ceb</code-block></chapter><chapter title="ខ្មែរ" id="lists-available-locales-km"><p>តើត្រូវបន្ថែមការធ្វើមូលដ្ឋានីយកម្មខ្មែរទៅកម្មវិធី Laravel យ៉ាងដូចម្តេច? ដើម្បីធ្វើដូច្នេះ អ្នកត្រូវហៅពាក្យបញ្ជាកុងសូលខាងក្រោម៖</p><p>How to add Khmer localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% km</code-block></chapter><chapter title="中文 (中国)" id="lists-available-locales-zh_CN"><p>如何向 Laravel 应用程序添加中文（中国）本地化？为此，您需要调用以下控制台命令：</p><p>How to add Chinese (China) localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% zh_CN</code-block></chapter><chapter title="中文 (中国香港特别行政区)" id="lists-available-locales-zh_HK"><p>如何向 Laravel 应用程序添加中文（中国香港特别行政区）本地化？为此，您需要调用以下控制台命令：</p><p>How to add Chinese (Hong Kong Sar China) localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% zh_HK</code-block></chapter><chapter title="中文 (台湾)" id="lists-available-locales-zh_TW"><p>如何為 Laravel 應用程式添加中文（台灣）在地化？為此，您需要呼叫以下控制台命令：</p><p>How to add Chinese (Taiwan) localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% zh_TW</code-block></chapter><chapter title="Hrvatski" id="lists-available-locales-hr"><p>Kako dodati hrvatsku lokalizaciju Laravel aplikaciji? Da biste to učinili, morate pozvati naredbu konzole ispod:</p><p>How to add Croatian localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% hr</code-block></chapter><chapter title="Čeština" id="lists-available-locales-cs"><p>Jak přidat českou lokalizaci do aplikace Laravel? Chcete-li to provést, musíte zavolat níže uvedený příkaz konzoly:</p><p>How to add Czech localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% cs</code-block></chapter><chapter title="Dansk" id="lists-available-locales-da"><p>Hvordan tilføjer man dansk lokalisering til en Laravel-applikation? For at gøre dette skal du kalde konsolkommandoen nedenfor:</p><p>How to add Danish localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% da</code-block></chapter><chapter title="डोगरी" id="lists-available-locales-doi"><p>लारावेल एप्लीकेशन च डोगरी लोकलाइजेशन गी किस चाल्ली जोड़ना ऐ? ऐसा करने लेई, तुसेंगी निचले कंसोल कमांड गी कॉल करना होग:</p><p>How to add Dogri localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% doi</code-block></chapter><chapter title="Nederlands" id="lists-available-locales-nl"><p>Hoe voeg ik Nederlandse lokalisatie toe aan een Laravel-applicatie? Om dit te doen, moet u de onderstaande consoleopdracht oproepen:</p><p>How to add Dutch localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% nl</code-block></chapter><chapter title="English" id="lists-available-locales-en"><p>How to add English localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% en</code-block></chapter><chapter title="Esperanto" id="lists-available-locales-eo"><p>Kiel aldoni esperantan lokalizon al Laravel-apliko? Por fari tion, vi devas voki la konzolan komandon sube:</p><p>How to add Esperanto localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% eo</code-block></chapter><chapter title="Eesti" id="lists-available-locales-et"><p>Kuidas lisada Laraveli rakendusele eestikeelset lokaliseerimist? Selleks peate helistama allolevale konsoolikäsule:</p><p>How to add Estonian localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% et</code-block></chapter><chapter title="Eʋegbe" id="lists-available-locales-ee"><p>Aleke woawɔ atsɔ Ewe localization akpe ɖe Laravel dɔwɔɖoɖo ŋu? Be nàwɔ esia la, ele be nàyɔ console sedede si le ete:</p><p>How to add Ewe localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% ee</code-block></chapter><chapter title="Suomi" id="lists-available-locales-fi"><p>Kuinka lisätä suomenkielistä lokalisointia Laravel-sovellukseen? Tätä varten sinun on kutsuttava alla oleva konsolikomento:</p><p>How to add Finnish localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% fi</code-block></chapter><chapter title="Français" id="lists-available-locales-fr"><p>Comment ajouter la localisation française à une application Laravel ? Pour ce faire, vous devez appeler la commande console ci-dessous :</p><p>How to add French localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% fr</code-block></chapter><chapter title="Frysk" id="lists-available-locales-fy"><p>Hoe kinne jo West-Fryske lokalisaasje tafoegje oan in Laravel-applikaasje? Om dit te dwaan, moatte jo it konsole-kommando hjirûnder skilje:</p><p>How to add Western Frisian localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% fy</code-block></chapter><chapter title="Galego" id="lists-available-locales-gl"><p>Como engadir localización ao galego a unha aplicación de Laravel? Para facelo, cómpre chamar ao comando da consola a continuación:</p><p>How to add Galician localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% gl</code-block></chapter><chapter title="ქართული" id="lists-available-locales-ka"><p>როგორ დავამატოთ ქართული ლოკალიზაცია ლარაველის აპლიკაციაში? ამისათვის თქვენ უნდა გამოიძახოთ კონსოლის ბრძანება ქვემოთ:</p><p>How to add Georgian localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% ka</code-block></chapter><chapter title="Deutsch" id="lists-available-locales-de"><p>Wie füge ich einer Laravel-Anwendung die deutsche Lokalisierung hinzu? Dazu müssen Sie den folgenden Konsolenbefehl aufrufen:</p><p>How to add German localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% de</code-block></chapter><chapter title="Deutsch (Schweiz)" id="lists-available-locales-de_CH"><p>Wie füge ich einer Laravel-Anwendung die deutsche (Schweiz) Lokalisierung hinzu? Dazu müssen Sie den folgenden Konsolenbefehl aufrufen:</p><p>How to add German (Switzerland) localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% de_CH</code-block></chapter><chapter title="Ελληνικά" id="lists-available-locales-el"><p>Πώς μπορώ να προσθέσω την ελληνική τοπική προσαρμογή σε μια εφαρμογή Laravel; Για να το κάνετε αυτό, πρέπει να καλέσετε την παρακάτω εντολή κονσόλας:</p><p>How to add Greek localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% el</code-block></chapter><chapter title="ગુજરાતી" id="lists-available-locales-gu"><p>Laravel એપ્લિકેશનમાં ગુજરાતી સ્થાનિકીકરણ કેવી રીતે ઉમેરવું? આ કરવા માટે, તમારે નીચેના કન્સોલ આદેશને કૉલ કરવાની જરૂર છે:</p><p>How to add Gujarati localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% gu</code-block></chapter><chapter title="Hausa" id="lists-available-locales-ha"><p>Yadda ake ƙara Hausa localization zuwa aikace-aikacen Laravel? Don yin wannan, kuna buƙatar kiran umarnin console da ke ƙasa:</p><p>How to add Hausa localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% ha</code-block></chapter><chapter title="ʻŌlelo Hawaiʻi" id="lists-available-locales-haw"><p>Pehea e hoʻohui ai i ka ʻōlelo Hawaiʻi i kahi palapala Laravel? No ka hana ʻana i kēia, pono ʻoe e kāhea i ke kauoha console ma lalo nei:</p><p>How to add Hawaiian localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% haw</code-block></chapter><chapter title="עברית" id="lists-available-locales-he"><p>כיצד להוסיף לוקליזציה עברית לאפליקציית Laravel? כדי לעשות זאת, עליך להתקשר לפקודת המסוף למטה:</p><p>How to add Hebrew localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% he</code-block></chapter><chapter title="हिन्दी" id="lists-available-locales-hi"><p>लारवेल एप्लिकेशन में हिंदी स्थानीयकरण कैसे जोड़ें? ऐसा करने के लिए, आपको नीचे दिए गए कंसोल कमांड को कॉल करना होगा:</p><p>How to add Hindi localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% hi</code-block></chapter><chapter title="Magyar" id="lists-available-locales-hu"><p>Hogyan lehet magyar lokalizációt hozzáadni egy Laravel alkalmazáshoz? Ehhez meg kell hívnia az alábbi konzolparancsot:</p><p>How to add Hungarian localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% hu</code-block></chapter><chapter title="Íslenska" id="lists-available-locales-is"><p>Hvernig á að bæta íslenskri staðfærslu við Laravel forrit? Til að gera þetta þarftu að hringja í stjórnborðsskipunina hér að neðan:</p><p>How to add Icelandic localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% is</code-block></chapter><chapter title="Igbo" id="lists-available-locales-ig"><p>Kedu otu esi etinye asụsụ Igbo na ngwa Laravel? Iji mee nke a, ịkwesịrị ịkpọ iwu njikwa n'okpuru:</p><p>How to add Igbo localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% ig</code-block></chapter><chapter title="Indonesia" id="lists-available-locales-id"><p>Bagaimana cara menambahkan lokalisasi bahasa Indonesia pada aplikasi Laravel? Untuk melakukan ini, Anda perlu memanggil perintah konsol di bawah ini:</p><p>How to add Indonesian localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% id</code-block></chapter><chapter title="Gaeilge" id="lists-available-locales-ga"><p>Conas logánú Éireannach a chur le feidhmchlár Laravel? Chun seo a dhéanamh, ní mór duit an t-ordú consól a ghlaoch thíos:</p><p>How to add Irish localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% ga</code-block></chapter><chapter title="Italiano" id="lists-available-locales-it"><p>Come aggiungere la localizzazione italiana a un'applicazione Laravel? Per fare ciò, è necessario chiamare il comando della console seguente:</p><p>How to add Italian localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% it</code-block></chapter><chapter title="日本語" id="lists-available-locales-ja"><p>Laravelアプリケーションに日本語ローカリゼーションを追加するにはどうすればよいですか?これを行うには、以下のコンソール コマンドを呼び出す必要があります。</p><p>How to add Japanese localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% ja</code-block></chapter><chapter title="ಕನ್ನಡ" id="lists-available-locales-kn"><p>Laravel ಅಪ್ಲಿಕೇಶನ್‌ಗೆ ಕನ್ನಡ ಸ್ಥಳೀಕರಣವನ್ನು ಹೇಗೆ ಸೇರಿಸುವುದು? ಇದನ್ನು ಮಾಡಲು, ನೀವು ಕೆಳಗಿನ ಕನ್ಸೋಲ್ ಆಜ್ಞೆಯನ್ನು ಕರೆಯಬೇಕು:</p><p>How to add Kannada localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% kn</code-block></chapter><chapter title="Қазақ Тілі" id="lists-available-locales-kk"><p>Laravel қолданбасына қазақша локализацияны қалай қосуға болады? Ол үшін төмендегі консоль пәрменін шақыру керек:</p><p>How to add Kazakh localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% kk</code-block></chapter><chapter title="Kinyarwanda" id="lists-available-locales-rw"><p>Nigute ushobora kongeramo Kinyarwanda muri progaramu ya Laravel? Kugirango ukore ibi, ugomba guhamagara itegeko rya konsole hepfo:</p><p>How to add Kinyarwanda localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% rw</code-block></chapter><chapter title="한국어" id="lists-available-locales-ko"><p>Laravel 애플리케이션에 한국어 지역화를 추가하는 방법은 무엇입니까? 이렇게 하려면 아래 콘솔 명령을 호출해야 합니다.</p><p>How to add Korean localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% ko</code-block></chapter><chapter title="Kurdî" id="lists-available-locales-ku"><p>Meriv çawa herêmîkirina kurdî li serîlêdana Laravel zêde bike? Ji bo kirina vê yekê, hûn hewce ne ku fermana konsolê ya jêrîn bang bikin:</p><p>How to add Kurdish localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% ku</code-block></chapter><chapter title="کوردیی ناوەندی" id="lists-available-locales-ckb"><p>چۆن لۆکاڵیزەکردنی کوردی ناوەندی بۆ ئەپڵیکەیشنی لاراڤێل زیاد بکەین؟ بۆ ئەم مەبەستە پێویستە پەیوەندی بە فەرمانی کۆنسۆڵەوە بکەیت لە خوارەوە:</p><p>How to add Central Kurdish localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% ckb</code-block></chapter><chapter title="Кыргызча" id="lists-available-locales-ky"><p>Laravel тиркемесинде кыргызча локализацияны кантип кошууга болот? Бул үчүн, сиз төмөнкү консол буйругун чакырышыңыз керек:</p><p>How to add Kyrgyz localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% ky</code-block></chapter><chapter title="ລາວ" id="lists-available-locales-lo"><p>ວິທີການເພີ່ມການແປພາສາລາວເຂົ້າໃນແອັບພລິເຄຊັນ Laravel? ເພື່ອເຮັດສິ່ງນີ້, ທ່ານຈໍາເປັນຕ້ອງໂທຫາຄໍາສັ່ງ console ຂ້າງລຸ່ມນີ້:</p><p>How to add Lao localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% lo</code-block></chapter><chapter title="Latviešu" id="lists-available-locales-lv"><p>Kā Laravel aplikācijai pievienot latviešu lokalizāciju? Lai to izdarītu, jums ir jāizsauc tālāk esošā konsoles komanda:</p><p>How to add Latvian localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% lv</code-block></chapter><chapter title="Lingála" id="lists-available-locales-ln"><p>Ndenge nini kobakisa localisation ya Lingala na application ya Laravel? Pona kosala yango, esengeli obenga commande ya console oyo ezali awa na se:</p><p>How to add Lingala localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% ln</code-block></chapter><chapter title="Lietuvių" id="lists-available-locales-lt"><p>Kaip Laravel aplikacijoje pridėti lietuvišką lokalizaciją? Norėdami tai padaryti, turite iškviesti žemiau esančią konsolės komandą:</p><p>How to add Lithuanian localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% lt</code-block></chapter><chapter title="Luganda" id="lists-available-locales-lg"><p>Oyinza otya okwongera Ganda localization ku nkola ya Laravel? Kino okukikola, olina okuyita ekiragiro kya console wansi:</p><p>How to add Ganda localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% lg</code-block></chapter><chapter title="Lëtzebuergesch" id="lists-available-locales-lb"><p>Wéi lëtzebuergesch Lokalisatioun an eng Laravel Applikatioun bäizefügen? Fir dëst ze maachen, musst Dir de Konsol Kommando hei drënner uruffen:</p><p>How to add Luxembourgish localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% lb</code-block></chapter><chapter title="Македонски" id="lists-available-locales-mk"><p>Како да додадете македонска локализација во апликацијата Laravel? За да го направите ова, треба да ја повикате командата на конзолата подолу:</p><p>How to add Macedonian localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% mk</code-block></chapter><chapter title="मैथिली" id="lists-available-locales-mai"><p>लारावेल एप्लीकेशन मे मैथिली लोकलाइजेशन कोना जोड़ल जाय? एकरा करय लेल, अहां के नीचा देल गेल कंसोल कमांड के कॉल करय पड़त:</p><p>How to add Maithili localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% mai</code-block></chapter><chapter title="Malagasy" id="lists-available-locales-mg"><p>Ahoana ny fomba hanampiana localization Malagasy amin'ny fampiharana Laravel? Mba hanaovana izany dia mila miantso ny baiko console eto ambany ianao:</p><p>How to add Malagasy localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% mg</code-block></chapter><chapter title="Melayu" id="lists-available-locales-ms"><p>Bagaimana untuk menambah penyetempatan Melayu pada aplikasi Laravel? Untuk melakukan ini, anda perlu memanggil arahan konsol di bawah:</p><p>How to add Malay localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% ms</code-block></chapter><chapter title="മലയാളം" id="lists-available-locales-ml"><p>ഒരു Laravel ആപ്ലിക്കേഷനിൽ മലയാളം പ്രാദേശികവൽക്കരണം എങ്ങനെ ചേർക്കാം? ഇത് ചെയ്യുന്നതിന്, നിങ്ങൾ താഴെയുള്ള കൺസോൾ കമാൻഡിലേക്ക് വിളിക്കേണ്ടതുണ്ട്:</p><p>How to add Malayalam localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% ml</code-block></chapter><chapter title="Malti" id="lists-available-locales-mt"><p>Kif iżżid il-lokalizzazzjoni Maltija ma' applikazzjoni Laravel? Biex tagħmel dan, trid issejjaħ il-kmand tal-console hawn taħt:</p><p>How to add Maltese localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% mt</code-block></chapter><chapter title="Māori" id="lists-available-locales-mi"><p>Me pehea te taapiri i te rohenga Maori ki te tono Laravel? Ki te mahi i tenei, me waea koe ki te whakahau papatohu i raro nei:</p><p>How to add Māori localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% mi</code-block></chapter><chapter title="मराठी" id="lists-available-locales-mr"><p>Laravel ऍप्लिकेशनमध्ये मराठी लोकॅलायझेशन कसे जोडायचे? हे करण्यासाठी, तुम्हाला खालील कन्सोल कमांडवर कॉल करणे आवश्यक आहे:</p><p>How to add Marathi localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% mr</code-block></chapter><chapter title="Manipuri" id="lists-available-locales-mni_Mtei"><p>ꯂꯥꯔꯥꯚꯦꯜ ꯑꯦꯞꯂꯤꯀꯦꯁꯟ ꯑꯃꯗꯥ ꯃꯅꯤꯄꯨꯔꯤ ꯂꯣꯀꯦꯂꯥꯏꯖꯦꯁꯟ ꯑꯁꯤ ꯀꯔꯝꯅꯥ ꯍꯥꯄꯆꯤꯅꯒꯅꯤ? ꯃꯁꯤ ꯇꯧꯅꯕꯒꯤꯗꯃꯛꯇꯥ, ꯅꯍꯥꯛꯅꯥ ꯃꯈꯥꯗꯥ ꯄꯤꯔꯤꯕꯥ ꯀꯟꯁꯣꯜ ꯀꯃꯥꯟꯗ ꯑꯁꯤ ꯀꯧꯕꯥ ꯃꯊꯧ ꯇꯥꯏ:</p><p>How to add Manipuri localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% mni_Mtei</code-block></chapter><chapter title="Монгол" id="lists-available-locales-mn"><p>Laravel програмд ​​хэрхэн монгол нутагшуулах вэ? Үүнийг хийхийн тулд та доорх консол командыг дуудах хэрэгтэй.</p><p>How to add Mongolian localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% mn</code-block></chapter><chapter title="မြန်မာ" id="lists-available-locales-my"><p>Laravel အပလီကေးရှင်းတွင် မြန်မာဘာသာပြန်ဆိုခြင်းကို မည်သို့ထည့်မည်နည်း။ ဒါကိုလုပ်ဖို့၊ အောက်ဖော်ပြပါ console command ကိုခေါ်ရပါမယ်။</p><p>How to add Burmese localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% my</code-block></chapter><chapter title="नेपाली" id="lists-available-locales-ne"><p>Laravel अनुप्रयोगमा नेपाली स्थानीयकरण कसरी थप्ने? यो गर्नको लागि, तपाईंले तलको कन्सोल आदेशलाई कल गर्न आवश्यक छ:</p><p>How to add Nepali localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% ne</code-block></chapter><chapter title="Norsk Bokmål" id="lists-available-locales-nb"><p>Hvordan legge til norsk bokmålslokalisering i en Laravel-applikasjon? For å gjøre dette, må du ringe konsollkommandoen nedenfor:</p><p>How to add Norwegian Bokmål localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% nb</code-block></chapter><chapter title="Norsk Nynorsk" id="lists-available-locales-nn"><p>Hvordan legge til norsk nynorsk lokalisering i en Laravel-applikasjon? For å gjøre dette, må du ringe konsollkommandoen nedenfor:</p><p>How to add Norwegian Nynorsk localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% nn</code-block></chapter><chapter title="Occitan" id="lists-available-locales-oc"><p>How to add Occitan localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% oc</code-block></chapter><chapter title="ଓଡ଼ିଆ" id="lists-available-locales-or"><p>ଏକ ଲାରାଭେଲ୍ ପ୍ରୟୋଗରେ ଓଡିଆ ଲୋକାଲାଇଜେସନ୍ କିପରି ଯୋଡାଯାଏ? ଏହା କରିବା ପାଇଁ, ଆପଣଙ୍କୁ ନିମ୍ନରେ ଥିବା କନସୋଲ୍ କମାଣ୍ଡକୁ କଲ୍ କରିବାକୁ ପଡିବ:</p><p>How to add Odia localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% or</code-block></chapter><chapter title="Oromoo" id="lists-available-locales-om"><p>Akkamitti Oromo localization gara application Laravel tti dabaluu dandeenya? Kana gochuuf ajaja konsolii armaan gadii waamuun si barbaachisa:</p><p>How to add Oromo localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% om</code-block></chapter><chapter title="پښتو" id="lists-available-locales-ps"><p>په لارویل اپلیکیشن کې د پښتو ځایی کولو څرنګوالی؟ د دې کولو لپاره ، تاسو اړتیا لرئ لاندې کنسول کمانډ ته زنګ ووهئ:</p><p>How to add Pashto localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% ps</code-block></chapter><chapter title="فارسی" id="lists-available-locales-fa"><p>چگونه بومی سازی فارسی را به اپلیکیشن لاراول اضافه کنیم؟ برای این کار باید دستور کنسول زیر را فراخوانی کنید:</p><p>How to add Persian localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% fa</code-block></chapter><chapter title="Filipino" id="lists-available-locales-fil"><p>Paano magdagdag ng lokalisasyon ng Filipino sa isang Laravel application? Upang gawin ito, kailangan mong tawagan ang console command sa ibaba:</p><p>How to add Filipino localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% fil</code-block></chapter><chapter title="Polski" id="lists-available-locales-pl"><p>Jak dodać polską lokalizację do aplikacji Laravel? Aby to zrobić, musisz wywołać poniższe polecenie konsoli:</p><p>How to add Polish localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% pl</code-block></chapter><chapter title="Português" id="lists-available-locales-pt"><p>Como adicionar localização em português a uma aplicação Laravel? Para fazer isso, você precisa chamar o comando do console abaixo:</p><p>How to add Portuguese localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% pt</code-block></chapter><chapter title="Português (Brasil)" id="lists-available-locales-pt_BR"><p>Como adicionar localização para português (Brasil) em uma aplicação Laravel? Para fazer isso, você precisa chamar o comando do console abaixo:</p><p>How to add Portuguese (Brazil) localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% pt_BR</code-block></chapter><chapter title="ਪੰਜਾਬੀ" id="lists-available-locales-pa"><p>ਲਾਰਵੇਲ ਐਪਲੀਕੇਸ਼ਨ ਵਿੱਚ ਪੰਜਾਬੀ ਸਥਾਨੀਕਰਨ ਕਿਵੇਂ ਜੋੜਿਆ ਜਾਵੇ? ਅਜਿਹਾ ਕਰਨ ਲਈ, ਤੁਹਾਨੂੰ ਹੇਠਾਂ ਦਿੱਤੀ ਕੰਸੋਲ ਕਮਾਂਡ ਨੂੰ ਕਾਲ ਕਰਨ ਦੀ ਲੋੜ ਹੈ:</p><p>How to add Punjabi localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% pa</code-block></chapter><chapter title="Runasimi" id="lists-available-locales-qu"><p>¿Imaynatataq huk aplicacion Laravelman quechua localizacionta yapasunman? Chaypaqqa, kay urapi consola kamachiyta waqyanayki tiyan:</p><p>How to add Quechua localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% qu</code-block></chapter><chapter title="Română" id="lists-available-locales-ro"><p>Cum se adaugă localizarea în limba română la o aplicație Laravel? Pentru a face acest lucru, trebuie să apelați comanda consolei de mai jos:</p><p>How to add Romanian localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% ro</code-block></chapter><chapter title="Русский" id="lists-available-locales-ru"><p>Как добавить русскую локализацию в приложение Laravel? Для этого вам нужно вызвать консольную команду ниже:</p><p>How to add Russian localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% ru</code-block></chapter><chapter title="संस्कृत भाषा" id="lists-available-locales-sa"><p>लारावेल् अनुप्रयोगे संस्कृतस्थानीयीकरणं कथं योजयितव्यम् ? एतत् कर्तुं भवद्भिः अधोलिखितं console आदेशं आह्वयितुं आवश्यकम् ।</p><p>How to add Sanskrit localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% sa</code-block></chapter><chapter title="Sardu" id="lists-available-locales-sc"><p>How to add Sardinian localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% sc</code-block></chapter><chapter title="Gàidhlig" id="lists-available-locales-gd"><p>Ciamar a chuireas tu sgìreachadh Gàidhlig na h-Alba ri tagradh Laravel? Gus seo a dhèanamh, feumaidh tu an t-òrdugh console a ghairm gu h-ìosal:</p><p>How to add Scottish Gaelic localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% gd</code-block></chapter><chapter title="Српски" id="lists-available-locales-sr_Cyrl"><p>Како додати српску локализацију у Ларавел апликацију? Да бисте то урадили, потребно је да позовете наредбу конзоле испод:</p><p>How to add Serbian localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% sr_Cyrl</code-block></chapter><chapter title="Serbian" id="lists-available-locales-sr_Latn"><p>How to add Serbian localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% sr_Latn</code-block></chapter><chapter title="Serbian (Montenegro)" id="lists-available-locales-sr_Latn_ME"><p>How to add Serbian (Montenegro) localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% sr_Latn_ME</code-block></chapter><chapter title="Chishona" id="lists-available-locales-sn"><p>Maitiro ekuwedzera Shona yemuno kuLaravel application? Kuti uite izvi, unofanirwa kufonera iyo console command pazasi:</p><p>How to add Shona localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% sn</code-block></chapter><chapter title="سنڌي" id="lists-available-locales-sd"><p>Laravel ايپليڪيشن ۾ سنڌي لوڪلائيزيشن کي ڪيئن شامل ڪجي؟ هن کي ڪرڻ لاء، توهان کي هيٺ ڏنل ڪنسول حڪم کي سڏڻ جي ضرورت آهي:</p><p>How to add Sindhi localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% sd</code-block></chapter><chapter title="සිංහල" id="lists-available-locales-si"><p>Laravel යෙදුමකට සිංහල දේශීයකරණය එකතු කරන්නේ කෙසේද? මෙය සිදු කිරීම සඳහා, ඔබ පහත කොන්සෝල විධානය අමතන්න:</p><p>How to add Sinhala localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% si</code-block></chapter><chapter title="Slovenčina" id="lists-available-locales-sk"><p>Ako pridať slovenskú lokalizáciu do aplikácie Laravel? Ak to chcete urobiť, musíte zavolať príkaz konzoly nižšie:</p><p>How to add Slovak localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% sk</code-block></chapter><chapter title="Slovenščina" id="lists-available-locales-sl"><p>Kako aplikaciji Laravel dodati slovensko lokalizacijo? Če želite to narediti, morate poklicati spodnji ukaz konzole:</p><p>How to add Slovenian localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% sl</code-block></chapter><chapter title="Soomaali" id="lists-available-locales-so"><p>Sidee loogu daraa meelaynta Soomaaliga codsiga Laravel? Si tan loo sameeyo, waxaad u baahan tahay inaad wacdo amarka console ee hoose:</p><p>How to add Somali localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% so</code-block></chapter><chapter title="Español" id="lists-available-locales-es"><p>¿Cómo agregar localización en español a una aplicación Laravel? Para hacer esto, necesita llamar al siguiente comando de la consola:</p><p>How to add Spanish localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% es</code-block></chapter><chapter title="Basa Sunda" id="lists-available-locales-su"><p>Kumaha cara nambihan lokalisasi basa Sunda kana aplikasi Laravel? Jang ngalampahkeun ieu, anjeun kedah nyauran paréntah konsol di handap ieu:</p><p>How to add Sundanese localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% su</code-block></chapter><chapter title="Kiswahili" id="lists-available-locales-sw"><p>Jinsi ya kuongeza ujanibishaji wa Kiswahili kwa programu ya Laravel? Ili kufanya hivyo, unahitaji kupiga simu amri ya console hapa chini:</p><p>How to add Swahili localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% sw</code-block></chapter><chapter title="Svenska" id="lists-available-locales-sv"><p>Hur lägger man till svensk lokalisering i en Laravel-applikation? För att göra detta måste du anropa konsolkommandot nedan:</p><p>How to add Swedish localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% sv</code-block></chapter><chapter title="Tagalog" id="lists-available-locales-tl"><p>Paano magdagdag ng lokalisasyon ng Tagalog sa isang Laravel application? Upang gawin ito, kailangan mong tawagan ang console command sa ibaba:</p><p>How to add Tagalog localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% tl</code-block></chapter><chapter title="Тоҷикӣ" id="lists-available-locales-tg"><p>Чӣ тавр маҳаллисозии тоҷикиро ба замимаи Laravel илова кардан мумкин аст? Барои ин, шумо бояд фармони консолро дар зер занг занед:</p><p>How to add Tajik localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% tg</code-block></chapter><chapter title="தமிழ்" id="lists-available-locales-ta"><p>லாராவெல் பயன்பாட்டில் தமிழ் உள்ளூர்மயமாக்கலை எவ்வாறு சேர்ப்பது? இதைச் செய்ய, நீங்கள் கீழே உள்ள கன்சோல் கட்டளையை அழைக்க வேண்டும்:</p><p>How to add Tamil localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% ta</code-block></chapter><chapter title="Татар" id="lists-available-locales-tt"><p>Ларавел кушымтасына татар локализациясен ничек өстәргә? Моның өчен сезгә түбәндәге консол командасына шалтыратырга кирәк:</p><p>How to add Tatar localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% tt</code-block></chapter><chapter title="తెలుగు" id="lists-available-locales-te"><p>లారావెల్ అప్లికేషన్‌కి తెలుగు స్థానికీకరణను ఎలా జోడించాలి? దీన్ని చేయడానికి, మీరు దిగువ కన్సోల్ ఆదేశానికి కాల్ చేయాలి:</p><p>How to add Telugu localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% te</code-block></chapter><chapter title="ไทย" id="lists-available-locales-th"><p>จะเพิ่มการแปลภาษาไทยให้กับแอปพลิเคชัน Laravel ได้อย่างไร ในการดำเนินการนี้ คุณต้องเรียกคำสั่งคอนโซลด้านล่าง:</p><p>How to add Thai localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% th</code-block></chapter><chapter title="ትግርኛ" id="lists-available-locales-ti"><p>ኣብ ላራቨል ኣፕሊኬሽን ከመይ ጌርና ትግርኛ ሎካላይዜሽን ንውስኸሉ? ነዚ ንምግባር ድማ ነዚ ኣብ ታሕቲ ዘሎ ናይ ኮንሶል ትእዛዝ ክትድውል ኣለካ፤</p><p>How to add Tigrinya localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% ti</code-block></chapter><chapter title="Türkçe" id="lists-available-locales-tr"><p>Laravel uygulamasına Türkçe lokalizasyonu nasıl eklenir? Bunu yapmak için aşağıdaki konsol komutunu çağırmanız gerekir:</p><p>How to add Turkish localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% tr</code-block></chapter><chapter title="Türkmen Dili" id="lists-available-locales-tk"><p>“Laravel” programmasyna türkmen lokalizasiýasyny nädip goşmaly? Munuň üçin aşakdaky konsol buýrugyna jaň etmeli:</p><p>How to add Turkmen localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% tk</code-block></chapter><chapter title="Akan" id="lists-available-locales-ak"><p>Sɛnea wɔde Akan localization bɛka Laravel application ho? Sɛ wobɛyɛ eyi a, ɛsɛ sɛ wofrɛ console ahyɛde a ɛwɔ ase ha no:</p><p>How to add Akan localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% ak</code-block></chapter><chapter title="ئۇيغۇرچە" id="lists-available-locales-ug"><p>Laravel قوللىنىشچان پروگراممىسىغا ئۇيغۇرچە يەرلىكلەشتۈرۈشنى قانداق قوشۇش كېرەك؟ بۇنىڭ ئۈچۈن تۆۋەندىكى كونترول سۇپىسىغا تېلېفون قىلىشىڭىز كېرەك:</p><p>How to add Uyghur localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% ug</code-block></chapter><chapter title="Українська" id="lists-available-locales-uk"><p>Як додати українську локалізацію до програми Laravel? Для цього вам потрібно викликати консольну команду нижче:</p><p>How to add Ukrainian localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% uk</code-block></chapter><chapter title="اردو" id="lists-available-locales-ur"><p>لاریول ایپلی کیشن میں اردو لوکلائزیشن کو کیسے شامل کیا جائے؟ ایسا کرنے کے لیے، آپ کو نیچے دی گئی کنسول کمانڈ کو کال کرنے کی ضرورت ہے۔</p><p>How to add Urdu localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% ur</code-block></chapter><chapter title="Uzbek" id="lists-available-locales-uz_Cyrl"><p>Laravel ilovasiga oʻzbekcha lokalizatsiyani qanday qoʻshish mumkin? Buning uchun siz quyidagi konsol buyrug'iga qo'ng'iroq qilishingiz kerak:</p><p>How to add Uzbek localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% uz_Cyrl</code-block></chapter><chapter title="O‘zbek" id="lists-available-locales-uz_Latn"><p>How to add Uzbek localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% uz_Latn</code-block></chapter><chapter title="Tiếng Việt" id="lists-available-locales-vi"><p>Làm cách nào để thêm bản địa hóa tiếng Việt vào ứng dụng Laravel? Để thực hiện việc này, bạn cần gọi lệnh console bên dưới:</p><p>How to add Vietnamese localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% vi</code-block></chapter><chapter title="Cymraeg" id="lists-available-locales-cy"><p>Sut i ychwanegu lleoleiddio Cymraeg at raglen Laravel? I wneud hyn, mae angen i chi ffonio'r gorchymyn consol isod:</p><p>How to add Welsh localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% cy</code-block></chapter><chapter title="Isixhosa" id="lists-available-locales-xh"><p>Ungongeza njani isiXhosa kwisicelo seLaravel? Ukwenza oku, kufuneka ufowunele lo myalelo we-console ungezantsi:</p><p>How to add Xhosa localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% xh</code-block></chapter><chapter title="ייִדיש" id="lists-available-locales-yi"><p>ווי אַזוי צו לייגן ייִדיש לאָוקאַלאַזיישאַן צו אַ לאַראַוועל אַפּלאַקיישאַן? צו טאָן דאָס, איר דאַרפֿן צו רופן די קאַנסאָול באַפֿעל אונטן:</p><p>How to add Yiddish localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% yi</code-block></chapter><chapter title="Èdè Yorùbá" id="lists-available-locales-yo"><p>Bawo ni a ṣe le ṣafikun isọdi ilẹ Yoruba si ohun elo Laravel kan? Lati ṣe eyi, o nilo lati pe aṣẹ console ni isalẹ:</p><p>How to add Yoruba localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% yo</code-block></chapter><chapter title="Isizulu" id="lists-available-locales-zu"><p>Ungangeza kanjani ukwenziwa kwasendaweni kwesiZulu kuhlelo lokusebenza lweLaravel? Ukuze wenze lokhu, udinga ukushayela umyalo we-console ngezansi:</p><p>How to add Zulu localization to a Laravel application? To do this, you need to call the console command below:</p><code-block lang="bash">%command-add% zu</code-block></chapter></snippet></topic>
